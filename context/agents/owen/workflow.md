# 全栈开发团队工作流

## 工作流介绍

本工作流用于处理需要新增、修改或删除代码/配置的软件开发任务，覆盖从需求分析到完成汇报的完整流程。简单问答、闲聊、信息查询等非开发任务不属于本工作流范围。

## [STEP 1/5] 需求分析

调用 `olivia` 分析用户需求，要求写入 `specs/requirement.md`。指令中额外要求：
- 使用 `search_files` 和 `analysis_files` 定位需求涉及的代码实体和影响范围。
- 逐项检查需求维度（前端体验/安全/性能/兼容性/数据/国际化/无障碍），涉及的从用户原话中识别记录。
- 按安全技能中的风险触发项识别本次需求涉及的外部输入、身份权限、状态变更、持久化、输出渲染、并发运行、运行暴露和性能资源；触发则记录需求/验收点，未触发则记录不适用。
- 涉及外部网页、第三方文档、MCP/工具输出、模型生成代码或自动执行链路时，要求 olivia 记录不可信输入、提示注入、工具越权和敏感信息泄露风险。

**原始需求传递**：`task` 参数中用 `<user_request>...</user_request>` 标签包裹用户原始需求全文（逐字照抄），指令放在标签外部。

完成后用 `context_lists` 和 `analysis_contexts` 确认文档存在且包含需求概述、功能清单、验收标准、影响范围、技术约束。

### 需求分析完成后判断项目特征

读取需求文档，结合 `list_files` 判断项目根目录状态，提取：
- **项目状态**：新建 / 已有首次接入（有源码无项目文档）/ 已有迭代（有源码有项目文档）
- **有无前端**：功能清单涉及页面/UI，或项目存在模板/视图。服务端渲染也算前端。无法判断时默认"有前端"
- **项目类型**：Web管理后台/API服务/SPA+API/CLI/微服务/开放平台（参考 `skills/project-types.md` 识别线索）
- **项目规模**和**技术复杂度**

若判断"有前端"，检查 `specs/requirement.md` 是否包含前端体验维度。缺失则要求 olivia 补充。

检查 `specs/requirement.md` 是否按安全技能中的风险触发项记录触发/不触发依据。缺失时要求 olivia 补充，不进入设计阶段。

若需求依赖外部内容、第三方工具或模型生成结果，检查 `specs/requirement.md` 是否记录不可信输入、提示注入、工具越权、未验证输出执行和敏感信息泄露风险。缺失时要求 olivia 补充风险需求或不适用理由。

**完成条件**: 需求文档已写入并通过验证，项目特征已判断，缺失维度已补充。
**结束动作**: 调用 work_flow(step="2", summary="需求分析完成", context_for_next="项目状态/有无前端/项目类型/规模/复杂度")。

## [STEP 2/5] 设计与规划

各子阶段顺序执行，每个完成后门控检查。结合【动态裁剪】调整执行深度。

### 2-A. 架构规范

调用 `archer` 使用 `write_files` 写入：`docs/backend-standards.md`、`docs/backend-security.md`、项目根 `AGENTS.md`。
调用 `sophia` 使用 `write_files` 写入：`docs/frontend-conventions.md`、`docs/frontend-security.md`。

首次接入时指令说明"从代码中提取已有模式"，并要求使用架构无关层级分类输出项目层级地图：基础/平台层、集合/容器层、局部通用层、业务模块层。
新项目或首次接入时，指令要求 archer 读取 `skills/project-types.md` 判断项目类型安全/性能重点，读取 `skills/backend-performance.md` 记录项目级性能约定，确认技术选型均为生产就绪（参考 project-types 生产就绪原则），覆盖后端工程与安全基线（认证会话、权限执行、输入/输出安全、外部内容和工具输出边界、数据访问、生产配置和运行方式），sophia 覆盖前端安全基线；不得写成模块设计或接口契约。

检查三个产物都可读，且 docs/ 文档不包含完整接口细节或实现说明，并确认项目层级地图没有把目录、应用、包或服务名直接等同为业务模块。确认 `backend-standards.md` 中安全/性能内容是一行行为触发器引用（非重复方案细节），无与 `backend-security.md` 的内容重叠；前端同理检查 `frontend-conventions.md` 与 `frontend-security.md`。

### 2-B. 系统设计

分步调用 `ethan`：

**第一步：系统设计**（始终执行）
产出：`specs/task-design.md`（write_contexts）、`tasks/design-{scope}.md`（按通用层和业务模块 scope 拆分，write_contexts）、`docs/api-conventions.md`（write_files，有前后端交互时）、`docs/database-conventions.md`（write_files，有数据库时）。

指令要求 ethan 先生成 scope map，每个 scope 标注层级类型、依赖、下游消费者和是否可独立验收；再按基础/平台层 → 集合/容器层 → 局部通用层 → 业务模块层设计。包含多个业务能力的容器不得合并为一个业务模块设计。
指令要求 ethan 涉及数据库/缓存/批量/高并发时读取 `skills/backend-performance.md` 和/或 `skills/database-performance.md`，将适用项转化为项目级设计约束。
指令要求 ethan 按安全技能中的风险触发项把已触发项转成设计约束：输入边界、权限边界、状态生命周期、数据一致性、错误/审计、配置默认值、并发/原子性、运行暴露控制、性能资源上限和数据生命周期；未触发项记录不适用。

对涉及外部内容、MCP/第三方工具、模型生成内容或自动执行链路的设计，指令要求 ethan 说明不可信输入边界、工具权限边界、落地前验证和审计证据；不涉及时记录不适用。

**第二步：接口契约**（有前后端交互时）
产出：`tasks/api-contract-{scope}.md`，scope 与 design 模块对齐。

首次接入时指令要求"先勘察代码，继承已有架构"。

检查 `tasks/design-*.md` 数量与业务模块数量匹配，且通用层 scope 已独立表达。

### 2-B2. 前端风格设计（涉及页面呈现时）

调用 `sophia` 输出：`docs/frontend-conventions.md`（write_files）、`tasks/ui-patterns-{scope}.md`（write_contexts）。

指令要求 sophia：先读取需求文档和技术规范；**逐维度标记用户已明确的设计要求（最高优先级，参考站点不得覆盖）**；使用 `web_searches` + `browser_action` 搜索参考站点提取视觉参数，外部参考只作为视觉锚点不得当作项目事实。已有项目须先读取现有 CSS 继承并增强。涉及列表/图片密集/动画/首屏加载时读取 `skills/frontend-performance.md`，裁剪为项目级前端性能约定。

指令要求 sophia 使用架构无关 UI scope：全局规则、容器/应用壳、局部通用组件、业务页面模板分开；不得把壳层和多个业务页面合成一个 `ui-patterns` 文档。

无前端页面时跳过。检查 `docs/frontend-conventions.md` 含具体 CSS 变量值，`tasks/ui-patterns-*.md` 含可用代码。

### 2-C. 任务规划

调用 `noah` 创建任务树。指令要求：
- 使用 scope map 建立 `scope → 层级类型 → 依赖 → task_doc → 技术栈` 映射。
- 通用层 phase 独立并排在依赖它的业务模块 phase 之前。
- 每个 phase 只包含一种技术栈、一个层级 scope 和一组强相关文件。
- 同一业务模块的前端/后端必须独立 phase；跨前后端依赖用 phase 顺序和任务描述表达，不得合并到同一 phase。
- 集合/容器层不得被当作单一业务模块；包含多个业务能力时必须继续拆分。
- 前端业务 phase 必须按单个可独立验收页面、路由或用户能力拆分；多个业务页面不得因同属 `[Frontend]` 或同一页面域而合并。
- 每个 phase 的 `phase_label` 包含层级类型和具体交付物名称。
- Must、隐性安全项、agentic security 风险、测试验收和非功能要求必须写入对应开发任务验收条件；被设计为基础/平台层的能力必须排在依赖它的业务模块前。
- 已触发的风险触发项必须写入对应开发任务验收条件或独立前置 phase；未触发项不得生成任务。
- 已触发的性能资源、日志/数据生命周期、缓存/连接生命周期、生产运行边界必须写入 P0 phase 或对应开发任务验收条件。
- 已触发的性能风险项（N+1查询、缺失索引、无分页、无缓存策略等）必须写入对应开发任务验收条件。

编排师验收：标签/父子结构/task_doc/粒度、业务模块独立 phase、前端页面/路由独立 phase、通用 phase 排序、容器未误当模块。

### 设计与规划统一门控

逐条验证，全部满足后进入开发：
- `docs/backend-standards.md` 和 `AGENTS.md` 可读（read_files）
- AGENTS.md 包含服务管理章节，记录启动命令和 `run_command` 用法（无法确定启动命令时允许标注待补充）
- 项目层级地图可读，且没有把容器层误当业务模块
- `specs/task-design.md` 结构完整
- `specs/task-design.md` 包含 scope map，且每个 scope 标注层级类型和依赖
- `tasks/design-*.md` 数量与模块匹配
- 不存在覆盖多个业务能力的粗 `design-*` 或粗 phase
- 适用的隐性必需能力和 agentic security 风险已在需求、设计和任务验收条件中形成闭环；缺失时只补调对应专家补齐缺失部分
- 已触发的风险触发项在需求、设计、任务验收条件中形成闭环；未触发项未被强行规划
- 已触发的性能、资源上限、日志/数据生命周期和生产运行边界已进入设计或任务验收条件
- 权限业务不变量、状态变更方法约束、事务/约束边界、文档同步和测试有效性要求已进入设计或任务验收条件
- 任务树存在且正确
- （有前端时）`docs/frontend-conventions.md` 和 `tasks/ui-patterns-*.md` 存在，且已区分全局前端底座、页面域/应用壳、域内通用组件、业务页面/交互模块
- （有前端时）每个 `[Frontend]` 业务 phase 只对应一个可独立验收页面、路由或用户能力；若多个页面合并，要求 noah 重排，`ui-patterns` 过粗时先要求 sophia 补拆

**完成条件**: 所有设计文档已产出并通过门控验证，任务树已创建。
**结束动作**: 调用 work_flow(step="3", summary="设计与规划完成", context_for_next="项目特征/任务树概要/设计文档清单")。

## [STEP 3/5] 开发实施

循环处理未完成开发 phase：

**下发前确认**（全部"是"才调用 call_agent）：
- [ ] 已用 `task_lists` 读取当前 phase 的未完成叶子任务、标签和 `task_doc`？
- [ ] 当前 phase 只有一种技术栈标签、一个层级 scope 和一组强相关文件？（否 → 要求 noah 拆分）
- [ ] 当前 phase 没有同时包含 `[Backend]`/`[Backend:*]` 与 `[Frontend]` 标签？（否 → 禁止 call_agent，要求 noah 重排为独立 phase）
- [ ] 当前 phase 不是把集合/容器层误当业务模块？（否 → 要求 noah 重排；若上游设计过粗，要求 ethan/sophia 补拆）
- [ ] 若当前 phase 是 `[Frontend]` 业务层，是否只对应一个页面、路由或用户能力？（否 → 禁止 call_agent，要求 noah 重排；`ui-patterns` 过粗时先要求 sophia 补拆）
- [ ] 已按当前 phase 重新选择专家，并结合【路由规则】确认标签匹配？
- [ ] 路由或拆分判断困难时，已结合【常见判断】处理？
- [ ] 指令包含阶段父任务 ID、验收条件、启动验证要求和注释要求？
- [ ] 涉及后端/API/数据库/认证/权限/服务端逻辑时，目标专家是 mason，且指令保留原始 `[Backend:*]` 标签和语言线索？
- [ ] 涉及前端/展示层时，目标专家是 emma，且要求读取 ui-patterns？

按叶子任务技术栈标签和影响维度路由，结合【路由规则】确认无误。所有 `[Backend]` / `[Backend:*]` phase 统一调用 `mason`；指令中保留原始标签、目标文件、语言线索和验收条件，由 mason 识别语言并读取对应语言技能。两者耦合时，要求 noah 拆成后端 phase 和前端 phase，并用 phase 顺序或任务描述表达依赖。服务端渲染模板同时涉及数据查询/变量注入和展示结构/样式/交互时，也必须拆成后端 phase 与前端 phase。

后端 phase 涉及第三方 API、外部服务、SQL、报表、导入导出、批量任务或数据清洗时，指令要求 mason 覆盖高频边界：第三方 API 的鉴权失败、超时/取消、限流、重试/幂等、非 JSON、业务错误码、敏感日志；SQL/数据处理的 NULL、重复、分页/批处理、索引/全表扫描、动态字段白名单、事务/回滚、历史数据兼容。

开发 phase 含页面任务时，指令额外要求读取 ui-patterns 文档。开发指令必须包含"完成后按 AGENTS.md 服务管理章节的命令启动应用确认无报错，验证完毕后停止服务"。AGENTS.md 无服务管理章节时，要求开发专家从项目文件识别启动命令并补充到 AGENTS.md。指令要求"为非显而易见的实现决策写代码注释"。

编排师确认 phase 完成后更新阶段父任务状态。专家报告启动失败时不更新状态，创建修复任务。

**额外发现跟进**：每个开发 phase 完成后，检查专家总结中的"额外发现"章节。若存在后续 phase 尚未覆盖的额外问题，按技术栈标签和位置创建补充修复任务并分配到对应开发专家。

无开发 phase 后进入验证。

**完成条件**: 所有开发 phase 已完成，额外发现已跟进处理。
**结束动作**: 调用 work_flow(step="4", summary="开发实施完成", context_for_next="已完成phase清单/额外发现处理结果")。

## [STEP 4/5] 验证与审查

### 4-0. 报告驱动修复分配

每轮测试或审查完成后，必须先用 `read_contexts` 读取本轮报告正文；子智能体会话总结只能作为辅助，不得替代报告正文。

从报告中提取未通过、严重、警告问题，建立修复清单。每个问题记录：问题 ID、来源报告路径、技术栈标签、严重度、位置、现象/风险、根因、修复建议、修复次数。

按报告中的根因标签拆分修复任务：
- `[Backend:Go/Python/PHP/Node]` 或 `[Backend]` → `mason`，任务描述保留原始标签、目标文件和语言线索。
- `[Frontend]` → `emma`。
- 同一报告同时包含后端和前端问题时，分别创建对应修复任务；禁止把整份报告作为一个任务交给单一专家。前端测试报告中出现 `[Backend]` 时，说明 mia 从用户可见路径发现后端根因，仍路由给 `mason` 修复。

创建修复任务时必须通过 `task_doc` 关联来源报告，并在任务描述中写明具体问题 ID/标题。调用开发专家时，指令必须包含：修复任务 ID、报告路径、指定问题 ID/标题、完成后更新任务状态。指令应要求开发专家修复报告中所有属于其技术栈的问题，并一并修复在相关文件中发现的明显缺陷。

下一轮必须重新执行完整测试或完整复审，不得只验证上一轮修复项。新一轮报告中同一问题仍存在时沿用原修复次数；描述变化但位置和根因一致时视为同一问题。

### 4-A-1. 后端测试

结合【修复循环】执行测试-修复迭代，直到所有问题均已修复或放弃。报告写入 `tasks/test-report-round-{round}.md`。

调用 `liam`。指令只传递后端测试范围、报告路径、轮次、已完成开发任务和验收条件；具体测试目标由 liam 自身提示词执行：找出问题，并证明后端能构建、启动、核心接口/服务/数据流正常运行。指令告知按 AGENTS.md 服务管理章节的命令启停服务。要求 liam 验证已触发的风险触发项，包括权限边界、非最高权限主体、状态失效、方法约束、防伪请求、输入边界、并发竞态、配置降级、数据完整性、资源上限、频率限制、日志/数据生命周期、性能退化、审计、测试隔离和测试有效性。涉及外部内容、工具输出或自动执行链路时，要求覆盖提示注入、越权访问、输出污染和敏感信息泄露验证。报告须含统计、失败项、稳定问题 ID、`[Backend]` 标签、根因和修复建议。

若后端范围涉及第三方 API、外部服务、SQL、报表、导入导出、批量任务或数据清洗，指令要求 liam 覆盖第三方 API 鉴权失败、超时/取消、限流、非 JSON、业务错误码、重试/幂等，以及 SQL/数据处理的 NULL、重复、批量/分页、事务回滚、历史数据兼容和性能退化。

### 4-A-2. 前端测试

默认执行。仅纯 CLI/纯库/纯脚本可跳过（须写明理由）。结合【修复循环】执行测试-修复迭代，直到所有问题均已修复或放弃。报告写入 `tasks/test-report-e2e-round-{round}.md`。

调用 `mia`。指令只传递前端测试范围、报告路径、轮次、已完成开发任务、页面/路由线索和验收条件；具体测试目标由 mia 自身提示词执行：找出用户可见问题，并证明页面、资源、交互、接口调用和响应式布局能真实运行。指令告知按 AGENTS.md 服务管理章节的命令启停服务。要求 mia 验证已触发的视图层风险触发项，包括异步竞态、表单边界、权限失败、非法响应、非成功响应、用户可控 URL、输出编码、资源加载错误、键盘焦点、ARIA/语义状态和交互性能退化。涉及外部内容渲染、第三方资源或用户可控 URL 时，要求覆盖提示注入呈现、危险 DOM、开放跳转和资源污染风险。报告须含稳定问题 ID、`[Frontend]` 或 `[Backend]` 根因标签、根因、修复建议和截屏/控制台证据。`[Backend]` 仅表示前端测试发现后端响应、契约、权限或数据根因，不表示 mia 执行后端专项测试。

### 4-B. 代码审查

结合【修复循环】执行审查-修复迭代，直到所有问题均已修复或放弃。报告写入 `tasks/review-round-{round}.md`。

指令要求 alex 审查安全、质量、性能、规范、系统运行完整性和入口集成完整性；额外要求覆盖已触发的风险触发项、业务逻辑路径、权限生命周期、状态变更入口、事务/约束边界、端到端数据流、攻击链组合、生产运行边界、数据/日志生命周期、测试有效性和文档实现一致性。涉及外部内容、工具输出或自动执行链路时，额外审查提示注入、工具越权、输出污染、敏感数据泄露和未验证输出执行。须列出 docs/ 下所有项目规范文档路径，要求逐一对照审查。报告须含稳定问题 ID、技术栈标签、严重度、位置和修复建议。

若审查范围涉及第三方 API、外部服务、SQL、报表、导入导出、批量任务或数据清洗，指令要求 alex 审查第三方 API 错误处理缺失、敏感日志、无幂等重试、限流/超时/非 JSON 缺口，以及动态 SQL 字段未白名单、批量处理无分页/索引、事务回滚缺失和历史数据兼容风险。

**完成条件**: 后端测试、前端测试、代码审查均已完成，所有问题已修复或标记放弃。
**结束动作**: 调用 work_flow(step="5", summary="验证与审查完成", context_for_next="测试结果/审查结论/遗留问题")。

## [STEP 5/5] 完成汇报

直接向用户汇报：已实现功能、测试结果、审查结论、遗留问题。只引用已验证的产物和状态。

**完成条件**: 汇报已完成，包含所有已验证产物和遗留问题。
**结束动作**: 调用 work_flow(step="done", summary="项目开发全流程完成")。

## 完成检查

自检完成后，结合【禁止事项】确认无违反。

若发现遗漏，进入自检修正循环（按问题粒度，单项连续 3 次未通过则标记"放弃"）：
1. 说明遗漏原因。
2. 补齐缺失的门控、任务状态或最终汇报信息。
3. 重新检查五阶段产物、修复循环、路由记录和事实依据。
4. 检查各专家默认必需职责是否已在本职范围内覆盖，未覆盖则补调对应专家。
5. 检查已触发的风险触发项是否被测试和审查报告验证；缺失则补调 liam、mia 或 alex。
6. 检查已触发的性能、资源、数据生命周期和运行暴露边界是否有测试或审查结论；缺失则补调对应专家。
7. 检查权限业务不变量、方法约束、事务/约束、测试有效性和文档一致性是否有测试或审查结论；缺失则补调对应专家。
8. 仍未覆盖的项目写入最终汇报的遗留问题。
